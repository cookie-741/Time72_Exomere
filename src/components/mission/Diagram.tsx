import diagram from "@/assets/images/diagram.png";

const Diagram = () => {
  return (
    <section>
      <img
        src={diagram}
        className="mx-auto object-cover"
      />
     
    </section>
  );
};

export default Diagram;
